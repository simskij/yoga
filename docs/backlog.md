# Backlog

## CLI

- `yo adr` command — new, list, show. for current project root, not for yo itself

## Dots

- SSH agent (`SSH_AUTH_SOCK`) support for age encryption — `filippo.io/age` requires direct private key material; agent support needs a custom `age.Identity` implementing ECDH via agent signing

## Docs

- C4 diagram generation (`just diagram`) — revisit when 4+ domains exist; best candidate is go-structurizr
