# Backlog

## CLI

- `yo adr` command — new, list, show. for current project root, not for yo itself

## Docs

- C4 diagram generation (`just diagram`) — revisit when 4+ domains exist; best candidate is go-structurizr
